name: Build and Release APK

on:
  push:
    tags:
      - 'v*' 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Restore Android Keystore
      run: |
        echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > app/release-key.jks
        
    - name: Build Signed APK
      run: |
        docker run --rm \
          -v $PWD:/application \
          -e KEYSTORE_PASSWORD="${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" \
          -e KEY_ALIAS="${{ secrets.ANDROID_KEY_ALIAS }}" \
          -e KEY_PASSWORD="${{ secrets.ANDROID_KEY_PASSWORD }}" \
          riderlty/vpoint-builder \
          gradle assembleRelease
          
    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y%m%d')" >> $GITHUB_OUTPUT
      
    - name: Rename APK file
      run: |
        cd app/build/outputs/apk/release
        for file in *.apk; do
          if [[ "$file" == *"release"* ]]; then
            new_name="vPoint.${{ steps.date.outputs.date }}.apk"
            cp "$file" "$new_name"
            echo "new_apk_path=app/build/outputs/apk/release/$new_name" >> $GITHUB_ENV
            echo "APK renamed to: $new_name"
            break
          fi
        done
        
    - name: Create Release
      id: create_release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: release-${{ steps.date.outputs.date }}
        release_name: "vPoint Release ${{ steps.date.outputs.date }}"
        body: |
          Automatic build of vPoint APK
          
          - Build date: ${{ steps.date.outputs.date }}
          - Commit: ${{ github.sha }}
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Upload APK to Release
      uses: softprops/action-gh-release@v1
      with:
        files: ${{ env.new_apk_path }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}